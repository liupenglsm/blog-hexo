<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
<%
	var title = page.title;
	if (is_archive()){
		title = 'Archives';
		if (is_month()){
			title += ': ' + page.year + '/' + page.month;
		} else if (is_year()){
			title += ': ' + page.year;
		}
	} else if (is_category()){
		title = 'Category: ' + page.category;
	} else if (is_tag()){
		title = 'Tag: ' + page.tag;
	}
	if (!title){
		title = 'Home';
	}
%>
	<title><%= title %> | <%= config.title %></title>
	<meta name="viewport" content="width=device-width">
	<link href="<%- config.root %>css/share/font-awesome.min.css" rel="stylesheet" type="text/css"> 

	<script src="<%- config.root %>js/jquery.min.js"></script>
	<script src="<%- config.root %>js/search.js"></script>
	<script src="<%- config.root %>js/script.js"></script>

<%- css('css/style/style') %>

	<link href="<%- config.root %>css/default.css" rel="stylesheet" type="text/css"> 

</head>